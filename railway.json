{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "flask db upgrade && gunicorn run:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --worker-class sync --max-requests 1000 --max-requests-jitter 50 --preload"
  }
}
